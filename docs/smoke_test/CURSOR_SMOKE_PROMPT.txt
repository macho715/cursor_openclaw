[SMOKE_TEST_CURSOR_GATEWAY_v1]
규칙:
- 외부 네트워크 사용 금지.
- 파일 생성/수정/삭제 금지(읽기만).
- 민감정보(토큰/키/로컬 경로)는 출력 금지(마스킹).

출력 요구:
1) "SMOKE_OK"를 첫 줄에 정확히 출력.
2) 내가 지금 쓰는 모델이 로컬 Gateway 기반인지, 아니면 다른 provider인지
   확실히 말할 수 없으면 "PROVIDER_UNKNOWN"라고 적고 넘어가라(추측 금지).
3) 다음 문장을 그대로 출력:
   "CURSOR->GATEWAY 요청 로그가 찍히면 E2E 통과"
