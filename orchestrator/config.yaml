telegram:
  bot_token: "YOUR_BOT_TOKEN_HERE"
  chat_id: "YOUR_CHAT_ID_HERE"
  # Optional: allow only these chat ids for /work command
  allowed_chat_ids: []
  # Set true to run polling mode by default
  polling_enabled: false

repository:
  root_path: "C:/Users/jichu/Downloads/cursor_full_setting_optionA/cursor_openclaw"

claude:
  # Non-interactive command that returns text to stdout.
  # Example below uses launch + prompt mode for Claude Code on Ollama.
  # You may replace with your own command if needed.
  ollama_command: "ollama launch claude --model kwangsuklee/SEOKDONG-llama3.1_korean_Q5_K_M:latest -- -p"
  prompt_template_path: "./prompts/claude_patch_prompt.txt"

settings:
  patch_dir: "./patches"
  report_dir: "./reports"
  task_prefix: "T{date}"
  auto_stash: false
